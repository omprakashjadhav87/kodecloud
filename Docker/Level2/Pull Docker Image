Pull busybox:musl image on App Server 2 in Stratos DC and re-tag (create new tag) this image as busybox:media.

Ans:- 

# SSH into App Server 2
ssh user@<app-server-2-ip>

# Pull the busybox:musl image
sudo docker pull busybox:musl

# Re-tag the image
sudo docker tag busybox:musl busybox:media

# Verify the new tag
sudo docker images

these steps should help you successfully pull and re-tag the Docker image. 
